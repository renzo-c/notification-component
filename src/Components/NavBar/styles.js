const boxStyles = {
  backgroundColor: "black",
  height: "60px",
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  py: ".3em",
  px: ".9em",
};

const menuStyles = {
  display: "flex",
  flex: 1,
  justifyContent: "space-between",
};

const linkStyles = {
  mx: "10px",
  display: "flex",
};

const noLinkStyles = {
  display: "flex",
};

export { boxStyles, menuStyles, linkStyles, noLinkStyles };
